1. ИнтерфейсПользователя(UserInterface)       - (предоставляет пользователю вводить арифметические выражения, выводит полученный результат и предлогает сохранить в файл)
          Поля:
           - String expression; - выражение пользователя
           - String result;     - записывается результат и арифметическое выражение
           

    Связанные сущности:
   - АрифметическоеВыражение
   - МенеджерРезультата
   - МенеджерФайла(ManagerFile)

   Функции:
   - Ввод пользователем    - setExpression(String expression)  (пользователь вводит строку из чисел и знаков, которая передается в Арифметическое выражение)
   - Вывод результата      - String printResult(ManagerResult result) (сущность МенеджерРезультата возвращает строку с результатом Арифметического выражения и с самим арифметическим выражением)

2. Арифметическое выражение(ArithmeticExpression)     - (это набор операндов и арифметических выраженийра разделенных знаками и скобками)
                     поля:
                      - String ArithmeticExpression; - ? точнить так как String не подходит

   Связанные сущности:
   - ИнтерфейсПользователя
   - Операнды
   - МатематическиеЗнаки
   - Группировка
   - Калькулятор

   Функции:
   - sendArithmeticExpression(UserInterface expression) (принимает строку из ИнтерфейсПользователя, передает значение в Операнды, в МатематическиеЗнаки и Группировку)
   - setArithmeticExpression (устанавливает арифметическое выражение после проверки в Операнды  в МатематическиеЗнаки и Группировку в поле ArithmeticExpression)
   - getArithmeticExpression (возвращает проверенное арифметическое выражение)

3.1. ArithmeticExpressionItem


3. Операнд  - (числа которые могут быть установлены в АрифметическихВыражениях калькулятора)
   - Арифметическое выражение
   - Ошибка

   Функции:
   - checkOperand (проверяет принятое значение на int или double, возвращает ошибку если введеный операнд не соответствует)
   - setOperand (устанавливает значение)
   - getOperand (возвращает установленное значение)

4. МатематическийЗнак - (математические знаки, которые возможно использовать в АрифметическихВыражениях калькулятора)
  - Арифметическое выражение
  - Ошибка

   Функции:
   - checkSign (проверяет  математический знак, возвращает ошибку если введеный знак не соответствует)
   - setSign (устанавливает знак)
   - getSign (возвращает установленный знак)

5. Группировка (определяет приоритет выполнения математического действия)
  - Арифметическое выражение
  - Операнд
  - МатематическийЗнак

   Функции:
   - checkGroup (проверяет правельность группировки в АрифметическомВыражении, возвращает ошибку если скобка одна или установлена не правильно)
   - setGroup (устанавливает знак)
   - getGroup (возвращает установленный знак)

6. Калькулятор (производит математические вычисления с учетом группировок)
   - Арифметическое выражение
   - Ошибка
   - Результат

   Функции:
   - calculate (производит математическое вычисление, в аргументы принимает Арифметическое выражение, возвращает Результат если все правильно, или ошибку)
                Ошибки:
                1. Передан null
                2. Не переданно Арифметическое выражение
                3. Арифметическое выражение состаит менее чем из 3 элементов
                4. В Арифметическое выражение на первом месте установлен знак
                5. В Арифметическое выражение на первом месте установлено не число
                6. В Арифметическое выражение на первом месте установлено число и на втором месте тоже число
                7. В Арифметическое выражение на первом месте установлено число на втором месте знак и на третем знак


7. Результат(Result) (принимает результат вычислений из сущности Калькулятор)
   - Калькулятор
   - МенеджерРезультата

   Функции:
   - setResult (устанавливает полученный из Калькулятор результат)
   - getResult (возвращает установленный результат число)


---- 8. МенеджерРезультата(ManagerResult) (передает результат и арифметическое выражение в интерфейс пользователя в виде строки)

              поля:
              - String result;  - записывает Результат и Арифметическое выражение в виде строки
   - Результат
   - ИнтерфейсПользователя

   Функции:
   -setResultAndArithmeticExpression     (запмсывает результат из Результат и Арифметическое выражение )
   -getResultAndArithmeticExpression     (возвращает Результат и Арифметическое выражение в виде строки )

9. МенеджерФайла(ManagerFile)  (сохраняет в файл Результат и Арифметическое выражение в виде строки)
   - ИнтерфейсПользователя

   Функции:
   - saveResultAndArithmeticExpression(UserInterface result,Path path)   (сохраняет сохраняет в файл Результат и Арифметическое)

---- 10. Путь к файлу(Path)
   - МенеджерФайла

   Функции:
   - setPath (устанавливает путь к файлу)
   - getPath (возвращает путь к файлу)










1. Арифметическое выражение - это набор арифметических выражений (числа, знаки, группировка)
2. Операция
3. Число
4. Группировка арифметического выраж.(операции в скобках)
5. Результат



2. Калькулятор ()
   - арифметическое выражение
   - функции:
     - вычислить выраж.(функция принимает арифметическое выражение, проверяет на соответствие,выдает ошибку если что то введено не правильно, производит вычисление, выдает резулитат)
       - входные параметры         -  Арифметическое выражение  (проверка на правильность введенного арифметического выражения )
       - выходной параметр         -  Результат  (математические вычисления арифметическое выражение и выдача результата)



Сущности:
1. Интерфейс пользователя - (пользователь вводит строку, которые преобразовываются в арифметическое выражение и передаются в calculate, возвращает результат который можно сохранить в файл)

   Функции:
   - Ввод пользователем  (пользователь вводит строку из чисел и знаков, которая передается в Арифметическое выражение)
   - Вывод результата    (сущность Результат возвращает строку с результатом Арифметического выражения)
   - Сохранение результата (результат передается в сущность Менеджер в котором сохраняется Результат)

2. Операнд
3. Знак
   -
   методы:
   -
2. Калькулятор












Сущности:

1. Интерфейс пользователя (пользователь вводит числа и знаки, которые преобразовываются в арифметическое выражение и передаются в calculate)
  - Ввод выражения пользователем  - Арифметическое выражение
  - Вывод результата              - Результат
  - Сохранение результата

     Поля:
      - num1 (int)
      - sign (String)
      - num2 (int)
      - result (int)
     Функции:
      - setNum1
      - setNum2
      - setSing
      - addNumNew
      - addSingNew
      - calculate
      - saveResult

2. Вывод результата (OutputResult)
   - Результат   - (Result)
   - Комментарий - (Comment)
   - Имя файла   - (String)
   - Путь к файлу -(String)

     Поля:
      - result (result)
      - text (Comment comment)
      - name (String)
      - path (String)
     Функции:
      - result
      - setText
      - setName
      - setPath
      - saveFile
      - cancel

3. Калькулятор (Calculator)
 (производит вычисление арифметического выражения)
   - Арифметическое выражение (ArithmeticExpression)
      -Число
      -Операция
      -Группировка выражения
   - Вычисления
   - Результат               (CalculateResult)

    Функции:
    - calculate

 4. Арифметическое выражение (ArithmeticExpression) сущность в которую передается введеное пользователем выражение (числа и знаки)
    Поля:
     -  number1Int   - содержит число (int)
     -  number2Int   - содержит число (int)
     -  number1Double   - содержит число (double)
     -  number2Double   - содержит число (double)
     -  sign      - установленный знак (String)
     Методы:

     -  setNumber1  - проверяет число, переводит в (int или double) и устанавливает в поле number1Int/Double
     -  setNumber2  - проверяет число, переводит в (int или double) и устанавливает в поле number1Int/Double
     -  setSing     - проверяет знак, передает в sing
     -  getNumber1  - предоставляет число из поля
     -  getNumber2
     -  getSing

 5. Результат (CalculateResult)
   -
 6. Ошибки (CalculatorException)

















1. Арифметическое выражение - это набор арифметических выражений (числа, знаки, группировка)
2. Операция
3. Число
4. Группировка арифметического выраж.(операции в скобках)
5. Результат



2. Калькулятор ()
   - арифметическое выражение
   - функции:
     - вычислить выраж.(функция принимает арифметическое выражение, проверяет на соответствие,выдает ошибку если что то введено не правильно, производит вычисление, выдает резулитат)
       - входные параметры         -  Арифметическое выражение  (проверка на правильность введенного арифметического выражения )
       - выходной параметр         -  Результат  (математические вычисления арифметическое выражение и выдача результата)



Интерфейс пользователя (пользователь вводит значения которые преобразовываются в арифметическое выражение)
  - Ввод выражения пользователем  - Арифметическое выражение
  - Вывод результата              - Результат
  - Сохранение результата




 Калькулятор
     - Интерфейс пользователя
     - Арифметическое выражение
        - Операция
        - Число
        - Групировка арифметического выражения
        - Результат
